resourceGroups = {
  "Project" = {                            # Key is the userDefinedString
    # tags = {                             # Uncomment tags if you need to add custom tags to resource group
    #     "example" = "value"
    # }
  }
}